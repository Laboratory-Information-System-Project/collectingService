<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.douzone.collectingservice.mapper.BarcodeMapper">
    <select id="findBarcode" resultType="String">
        select max(cg.barcode)
        from collecting cg;
    </select>

    <select id="findAllByPrescribeCode" resultType="String">
        select c.barcode
        from collecting c
        where prescribe_code in (
        <foreach collection="prescribeList" item="item" separator=",">
            #{item}
        </foreach>
        )
    </select>

    <insert id="initBarcode">
        {call sequence('barcode')}
    </insert>

    <insert id="insertNewBarcode">
        <foreach collection="prescribeCodeList" item="item" separator=";">
        insert into collecting (barcode, status_code, barcode_dt, barcode_printer_id, prescribe_code)
             select
            (
            select concat(${today},
                         (select LPAD ((select nextval('barcode') from dual), 5, 0)
                            from dual)
            ) from dual ),'B', now(), 'N001', ${item}
            from dual
            where isnull( (select c.barcode
            from collecting c
            where prescribe_code = ${item} ))
        </foreach>
    </insert>

    <update id="deleteBarcode">
    update collecting
       set cancel_barcode_dt = now(), status_code= 'X'
     where barcode in (
         <foreach collection="barcodeList" item="item" separator=",">
             #{item}
         </foreach>
         )
    </update>
</mapper>


        <!--        CREATE TABLE `collecting` (-->
        <!--        `barcode`	varchar(30)	NOT NULL,-->
        <!--        `status_code`	varchar(2)	NOT NULL,-->
        <!--        `barcode_dt`	datetime	NOT NULL,-->
        <!--        `collecting_dt`	datetime	NULL,-->
        <!--        `barcode_printer_id`	varchar(20)	NOT NULL,-->
        <!--        `collector_id`	varchar(20)	NULL,-->
        <!--        `prescribe_code`	bigint	NOT NULL-->
        <!--        );-->